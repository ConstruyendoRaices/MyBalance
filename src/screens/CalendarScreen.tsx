import { View, Text } from 'react-native';

const CalendarScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Pantalla del Calendario</Text>
  </View>
);

export default CalendarScreen;