import { View, Text } from 'react-native';

const CalendarScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Pantalla del Formulario</Text>
  </View>
);

export default CalendarScreen;